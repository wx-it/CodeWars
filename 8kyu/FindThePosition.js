//When provided with a letter, return its position in the alphabet.

function position(letter) {
    let output = letter.charCodeAt(letter) - 96;
           return "Position of alphabet:" + " " + output;
  }
